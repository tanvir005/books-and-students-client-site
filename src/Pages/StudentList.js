import React from 'react';
import Grid from '@mui/material/Grid';

const StudentList = () => {
    return (
        <Grid item xs={8}>
            student list
        </Grid>
    );
};

export default StudentList;